<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= success ? 'Registration Successful' : 'Verification Failed' %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="bg-light d-flex justify-content-center align-items-center min-vh-100">
  <div class="text-center">
    <div class="card shadow p-5" style="border-radius: 20px;">
      <% if (typeof success !== 'undefined') { %>
        <div class="mb-3 <%= success ? 'text-success' : 'text-danger' %>" style="font-size: 48px;">
          <%= success ? "✅" : "❌" %>
        </div>
      <% } %>
      <h4 class="mb-2">
        <%= success ? 'Registration Successful!' : 'Verification Failed' %>
      </h4>

      <p class="text-muted">
        <%= message || (success ? 'Your account has been verified and registered successfully.' : 'An error occurred during verification.') %>
      </p>

      <% if (success) { %>
        <a href="/users/login" class="btn btn-success mt-3">Go to Login</a>
      <% } else { %>
        <a href="/users/signup" class="btn btn-danger mt-3">Try Again</a>
      <% } %>
    </div>
  </div>
</body>
</html>
